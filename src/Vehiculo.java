public interface Vehiculo {
    //esta es la segunda interface que conecta la interface anterior con su implementacion y los tipos de vehiculos (clases)
    int calcularComparendo(int velocidad);
    String enviarCorreoFotomulta();
}
